# values_1.yaml - Minimal SonarQube values for AKS

service:
  type: LoadBalancer
  port: 9000

ingress:
  enabled: false

postgresql:
  postgresqlUsername: sonarqube
  postgresqlPassword: "YourSecurePassword123!"
  persistence:
    enabled: true
    size: 8Gi

persistence:
  enabled: true
  size: 10Gi

monitoringPasscode: "YourStrongPasscode123!"

